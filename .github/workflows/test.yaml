name: terratorch tuning toolkit

on: 
  push:
    branches:
      - main
    paths-ignore:
      - 'docs/**'
      - '*.md'
      - 'tests/resources/**'
      - 'examples/**/*'
      - '**/*.svg'
      - '**/*.png'
      - '**/*.txt'
      - '**/*.ipynb'
  pull_request:
    branches:
      - main
    paths-ignore:
      - 'docs/**'
      - '*.md'
      - 'tests/resources/**'
      - 'examples/**/*'
      - '**/*.svg'
      - '**/*.png'
      - '**/*.txt'
      - '**/*.ipynb'
jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 40
    strategy:
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    env:
      R_TESTS_VERBOSE: 2
    steps:
      - name: Clone repo
        uses: actions/checkout@v4
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: 'pip'
      - name: Install dependencies
        run: |
          #python -m pip install --upgrade pip
          pip install pip==24.2
          pip --version
          pip install -e .[wxc,test]
          pip install pytest-testmon
          # Installing dependencies required to test Galileo (temporary)
          pip install git+https://github.com/Joao-L-S-Almeida/terratorch-galileo.git
      - name: Clean pip cache
        run: pip cache purge
      - name: List pip dependencies
        run: pip list
      - name: Cleaning cache
        run: pip cache purge
      - name: Test with pytest
        run: |
          pytest -s -v --testmon tests
          mkdir testmondir
          mv .testmondata testmondir/testmondata
          ls -a testmondir
      - name: Uploading artifacts
        #if: ${{ hashFiles('.testmondata') == '' }}
        uses: actions/upload-artifact@v4
        with:
          if-no-files-found: ignore
          name: testmondata-artifact-${{ matrix.python-version }}
          path: testmondir/testmondata
      - name: Downloading artifacts
        uses: actions/download-artifact@v5
        with:
          run-id: ${{ github.event.workflow_run.id }}
          github-token: ${{ github.token }}
          name: testmondata-artifact-${{ matrix.python-version }}
          path: .
      - name: list files
        run: ls -a .testmon*


